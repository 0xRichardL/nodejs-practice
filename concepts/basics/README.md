# Basics

Use this directory for focused JavaScript, TypeScript, ES module, standard
library, file-system, CLI, and error-handling demonstrations.

Each runnable topic should live in its own directory with a `main.ts` entry
point.
